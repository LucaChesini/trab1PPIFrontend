import Card from "./Card";

function getCards() {
    cards = [
        'card1',
        'card2'
    ]

    return cards;
}

const Coluna = () => {
    return (
        <div>
            {/* {getCards().map((card) => (
                <Card />
            ))} */}
            "Teste"
        </div>
    )
}

export default Coluna;